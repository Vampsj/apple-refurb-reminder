# Apple Refurb Reminder 产品需求文档 V0.2

## 一、项目背景

用户希望购买符合特定配置的 Apple 日本整备品 MacBook Pro。Apple 官方商店不提供库存提醒，用户只能频繁刷新页面，因此需要一个可长期运行的自动监控工具。

第一阶段仅供单个用户在本地使用，但核心逻辑应与具体地区页面和通知渠道解耦，方便未来增加其他地区、产品和通知渠道。

## 二、目标与完成标准

### 2.1 产品目标

持续观察 Apple 日本整备品商店。发现符合订阅条件的新上架商品后，通过 Discord 和 Email 尽快通知用户，同时避免重复或过期提醒。

### 2.2 里程碑 A：首条提醒

首个里程碑的目标是尽快验证真实链路：

- 成功检查 Apple 日本真实页面；
- 正确解析目录页和商品详情页；
- Discord 测试消息发送成功；
- 有真实匹配库存时发送真实库存通知；
- 无真实匹配库存时，仅在用户显式执行测试命令后发送明确标注为测试的无库存消息。

测试消息的标题和正文必须包含 `TEST`，正文说明“今日无匹配的真实库存”，不得包含虚构商品、价格或链接。测试不得创建上架事件，也不得污染正式去重状态。

### 2.3 里程碑 B：MVP 完成

MVP 只有在以下条件全部满足后才算完成：

- Discord 与 Email 均可用；
- 去重、下架确认、重新上架、批次投递、重试及过期规则生效；
- 本地状态可安全跨重启恢复；
- 日志、故障通知、CLI 和 macOS `launchd` 配置可用；
- 自动化测试与真实手动验收通过；
- 文档足以让用户从空环境完成配置和启动。

## 三、MVP 范围

### 3.1 包含

- Apple 日本整备品商店；
- MacBook Pro 精确配置订阅；
- 目录页与商品详情页的两阶段观察；
- Discord Webhook；
- SMTP Email；
- 本地 CLI；
- 本地 JSON 状态持久化；
- macOS `launchd` 守护配置；
- 日语通知；
- 结构化日志和运行故障通知。

### 3.2 不包含

- Docker；
- Web UI；
- 数据库；
- 多国家；
- 多用户；
- WhatsApp；
- 动态插件安装；
- 浏览器自动化；
- 价格变化提醒；
- 电脑关机或睡眠期间的持续监控保证。

## 四、地区、语言与时间

MVP 固定使用：

- Storefront Region：`JP`
- Storefront Locale：`ja-JP`
- Currency：`JPY`
- Display timezone：`Asia/Tokyo`

地区与语言是独立概念。未来增加其他地区时，地区可以提供默认语言，但用户应能单独覆盖语言。

商品名称保留 Apple 页面原文，不进行机器翻译。通知字段标签使用日语，价格使用日元格式。内部时间使用带时区的 UTC；用户可见时间默认显示 JST。日志时间使用包含偏移量的 ISO 8601 格式。

商品发现时间与消息发送时间必须分别记录。

## 五、订阅规则

### 5.1 MVP 目标配置

第一条订阅采用精确匹配：

- 产品：MacBook Pro
- 屏幕尺寸：14 英寸
- 芯片：M5 Pro
- CPU：15 核
- GPU：16 核
- 内存：48GB Unified Memory
- SSD：1TB

以下属性不参与筛选：

- 颜色；
- 价格；
- 键盘布局。

不参与筛选的属性若能可靠解析，应在通知中展示。不同空格、大小写、连字符和日英文字样可先标准化，但标准化后仍必须精确匹配。`512GB`、`2TB`、16 英寸或其他核心数均不匹配。

### 5.2 订阅身份

每条订阅必须具有用户指定且稳定的唯一 `id`，例如 `m5-pro-48gb-1tb-14`.

去重状态按“订阅 ID + 商品条目标识”保存。修改同一订阅的筛选条件后：

- 原本不匹配、现在首次匹配的在售商品应立即提醒；
- 始终匹配的在售商品不得重复提醒；
- 删除订阅后暂时保留其历史状态；
- 重新使用同一 ID 时延续历史；
- 需要全新历史时应使用新 ID 或显式重置状态。

## 六、配置管理

### 6.1 文件边界

- `.env`：密钥和运行环境配置；
- `subscriptions.yaml`：结构化订阅规则；
- `.env.example` 与 `subscriptions.example.yaml`：可提交的示例；
- 真实 `.env`：必须被 Git 忽略。

示例运行配置：

```env
APPLE_REGION=JP
APPLE_LOCALE=ja-JP
DISPLAY_TIMEZONE=Asia/Tokyo
CHECK_INTERVAL_SECONDS=600
STATE_FILE=./data/state.json

DISCORD_WEBHOOK=...

SMTP_HOST=...
SMTP_PORT=...
SMTP_USERNAME=...
SMTP_PASSWORD=...
EMAIL_FROM=...
EMAIL_TO=...
```

示例订阅：

```yaml
schema_version: 1
subscriptions:
  - id: m5-pro-48gb-1tb-14
    product: MacBook Pro
    display_size_inches: 14
    chip: M5 Pro
    cpu_cores: 15
    gpu_cores: 16
    memory_gb: 48
    storage: 1TB
```

### 6.2 校验与加载

- 配置只在启动时加载，不支持热加载；
- 修改配置后必须显式重启程序；
- 未知字段、缺少必需字段、重复订阅 ID、空订阅列表或含糊单位均导致启动失败；
- 配置必须包含 schema 版本；
- `CHECK_INTERVAL_SECONDS` 默认值为 600，必须是整数且不得低于 60；
- 程序不得静默修正非法值；
- 启动日志只显示脱敏配置摘要，不得显示密钥；
- 代码中不得硬编码账号、密码或密钥。

## 七、库存观察

### 7.1 两阶段观察

总页面不包含完整的 RAM、SSD 等规格，因此每轮检查分为：

1. **目录观察**：取得当前在售条目的商品名称、价格、链接和稳定标识；
2. **详情解析**：打开候选商品详情页，取得芯片、CPU、GPU、内存、SSD、尺寸及其他可用属性；
3. **订阅匹配**：详情中的所有必需属性明确解析后，才进行严格匹配；
4. **批次通知**：整轮可信检查结束后，立即汇总本轮新上架事件并发送。

首次看到某个商品条目时读取详情并缓存规格。商品持续在售时可复用规格缓存，但目录页中的当前价格应更新。商品确认下架后重新出现时，必须重新读取详情，防止链接或内容被复用。

详情请求或解析失败时，该条目标记为“无法评估”，本轮不提醒，下轮继续尝试。无法评估不等于不匹配。

### 7.2 商品身份

商品身份按以下优先级确定：

1. Apple 商品 ID 或 part number；
2. 移除查询参数、片段和跟踪参数后的规范商品链接。

名称、价格和规格不得作为商品身份。两个不同 Apple 商品 ID 即使规格相同，也视为不同商品条目。

如果既无稳定 ID，也无可靠链接，该商品可以记录，但不得创建提醒，以免每轮重复通知。

### 7.3 页面访问约束

- 仅访问 Apple 公开、无需登录的页面或页面使用的公开 JSON 数据源；
- 不登录 Apple 账号；
- 不绕过验证码、限流或其他访问控制；
- 遇到验证码、限流或未知访问控制时退避并创建运行故障；
- MVP 不使用 Playwright、Selenium 等浏览器自动化；
- 只有确认必需规格必须执行 JavaScript 才能取得时，才重新评估浏览器方案；
- Apple 页面逻辑必须封装在日本 Storefront Adapter 内。

## 八、检查调度与请求重试

### 8.1 调度

- 默认每 10 分钟检查一次；
- 一轮完成后等待配置间隔，再启动下一轮；
- 同一进程内检查轮次绝不重叠；
- 详情请求可受控并发，默认最大并发数为 3；
- 检查耗时异常时记录警告，不启动重叠轮次；
- Mac 唤醒或检测到长时间暂停后立即检查一轮；
- 不补跑睡眠期间错过的所有轮次；
- 唤醒后的检查完成后恢复正常间隔。

### 8.2 Apple 请求重试

- 每个目录页或详情页请求最多尝试 3 次；
- 连接超时默认 5 秒，完整响应超时默认 20 秒；
- 使用指数退避和随机抖动；
- 网络超时、`429`、`500`、`502`、`503`、`504` 可重试；
- 大多数其他 `4xx` 不重试；
- 存在 `Retry-After` 时优先遵守，但单轮不得无限等待；
- 三次失败后，相应观察视为不可信，单次异常不得导致主循环退出；
- 详情页最终失败只影响该条目，不否定同轮其他成功条目。

## 九、可信观察与下架确认

一次目录检查只有通过以下完整性条件，才能作为可信库存观察：

- HTTP 状态与内容类型符合预期；
- 响应不是登录页、验证码页、限流页或错误页；
- 预期目录或数据结构存在；
- 分页或加载流程完整结束；
- 解析器未遇到未知结构或异常。

解析商品数为 0 仍可能是可信观察。商品数量较上轮大幅减少只能触发警告，不能单独判定观察失败。

下架规则：

- 已在售商品连续两次从可信库存观察中缺席，才确认下架；
- 请求失败、页面异常或不可信观察不累计缺席次数；
- 只缺席一次后再次出现，仍视为持续在售，不重新提醒；
- 确认下架后再次出现，创建新的上架事件并重新读取详情。

持续在售期间的价格变化只更新状态并记录日志，不创建新提醒。

## 十、首次运行、重启与状态异常

- 真正首次运行时，当前匹配商品全部视为新的上架事件；
- 正常重启读取持久化状态，持续在售商品不得重复提醒；
- 状态文件损坏或 schema 不兼容时，不得静默当作首次运行；
- 状态异常时保留损坏文件、记录严重错误并暂停通知，但可继续观察和记录库存；
- 用户只能通过明确的 CLI 操作重置状态；
- 重置后下一轮会把当前匹配商品视为新上架；
- 同一状态文件只允许一个进程持有锁；第二个实例应拒绝启动。

## 十一、通知行为

### 11.1 渠道与独立状态

MVP 启用：

- Discord Webhook；
- SMTP Email。

每个通知渠道独立保存投递状态。Discord 成功而 Email 失败时，不得重复发送 Discord；Email 可继续重试。一个渠道暂停不得影响库存检查或另一个渠道。

Discord 返回明确成功响应、或 SMTP 服务器接受邮件后，视为该渠道投递成功。程序不承诺用户已经阅读消息。

### 11.2 批次

- 同一轮可信检查中发现的所有新上架事件合并为一个通知批次；
- 商品使用日语箇条書き展示；
- 商品级匹配和去重独立于批次发送；
- 同一商品匹配多条订阅时只展示一次，并标注匹配的订阅；
- 正常情况下 Discord 一条消息、Email 一封邮件；
- 超过渠道硬性长度限制时，Discord 可拆成 `1/N`、`2/N` 等编号分段；
- 所有分段成功后，该渠道的批次才算成功；
- 重试只发送失败分段，不重复已成功分段；
- Email 不因 Discord 分段而强制拆分。

“立即通知”定义为：整轮可信检查及候选详情评估结束后，不增加额外聚合等待，立即发送该轮批次。某一详情页最终失败不得无限阻塞其他已确认匹配商品。

### 11.3 商品通知内容

每个商品至少包含：

- 商品名；
- 屏幕尺寸；
- CPU / GPU；
- 内存；
- SSD；
- 价格；
- 商品链接；
- 匹配的订阅；
- 检出日時；
- 经过重试时的送信日時。

商品名称保留 Apple 日文原文。Discord 使用 Markdown；Email MVP 使用可读的纯文本箇条書き。商品链接使用规范链接。

### 11.4 投递重试与过期

- 首次投递立即尝试；
- 短暂故障按约 1、5、15、30、60 分钟退避，之后最多每 60 分钟一次；
- 重试加入随机抖动；
- Discord `429` 遵守 `Retry-After`；
- 网络错误和服务端 `5xx` 可重试；
- 无效 Webhook、SMTP 认证失败等明确配置错误暂停对应渠道；
- 配置改变并重启且校验成功后可恢复；
- 重试前重新检查批次中的商品；
- 已确认下架的条目从待发送内容中移除；
- 批次全部条目均确认下架时，未完成投递标记为过期并停止；
- 商品以后重新出现时创建新的批次。

## 十二、运行故障通知

运行故障通知与库存通知分开，不合并。

- Discord 故障时优先通过 Email 报告；
- Email 故障时优先通过 Discord 报告；
- 两个渠道都失效时只能写控制台和日志；
- 同一故障首次发生时通知一次，持续期间不重复轰炸；
- 故障恢复后发送一次恢复通知；
- 普通 Apple 检查连续失败 3 轮后升级为运行故障；
- 状态文件损坏、凭据无效、全局页面结构未知等严重故障立即升级。

单个详情页无法评估时：

- 第一次和第二次只记录警告；
- 同一条目连续 3 次可信目录观察都无法解析必需字段时创建运行故障；
- 后续成功解析时发送恢复通知；
- 同轮至少 3 个详情页失败且失败比例达到 50% 时，视为批量结构性失败并立即告警。

## 十三、状态持久化与清理

MVP 使用本地 JSON 状态文件，不使用数据库。

要求：

- 默认路径 `./data/state.json`，允许配置；
- 状态包含 schema 版本；
- 同目录临时文件写入完成并刷新后，再原子替换正式文件；
- 更新前保留最近的有效备份 `state.json.bak`；
- 使用进程锁防止多个实例同时修改；
- 密钥不得进入状态文件；
- 状态文件只保存恢复运行所需的当前状态，不作为历史事件日志；
- 已完成通知批次保留 7 天；
- 已确认下架商品状态保留 30 天；
- 已删除订阅状态保留 30 天；
- 当前在售、等待重试或状态未决的数据不得清理；
- 清理后的商品以后重新出现时按新上架处理。

## 十四、日志与诊断

常规日志记录结构化摘要，不保存每次完整 HTML：

- 程序启动、停止和加载的脱敏配置摘要；
- 每轮检查开始、结束和持续时间；
- 请求 URL、HTTP 状态、耗时和响应大小；
- 目录解析商品数；
- 成功解析、无法评估和匹配数量；
- 通知批次、分段和各渠道投递结果；
- 价格变化；
- 错误、重试和运行故障生命周期。

要求：

- 同时输出到控制台和日志文件；
- 日志按天轮转，默认保留 14 天；
- Webhook、SMTP 密码、令牌和其他密钥必须脱敏；
- 状态文件与日志使用不同生命周期；
- 仅在未知结构等异常情况下保存大小受限、经过脱敏的诊断快照；
- 不得把完整 HTML 持续写入常规日志。

## 十五、CLI

MVP 提供以下命令：

- `validate-config`：离线校验配置，不访问网络；
- `check-once`：真实检查一次并输出结果，默认不发通知、不修改正式状态；显式使用 `--send-test-if-empty` 时，若本轮可信检查无匹配库存，则发送“今日无匹配库存”的 TEST 消息；
- `test-notifications`：向已配置渠道发送明确标注为测试的消息；
- `run`：长期监控，更新正式状态并发送提醒；
- `reset-state`：显式重置状态，交互确认；自动化使用时必须显式传入 `--yes`；
- `status`：显示最近成功检查时间、当前匹配数、渠道健康、未完成投递、当前配置摘要和配置指纹。

建议首次验收顺序：

```text
validate-config → test-notifications → check-once → run
```

## 十六、进程生命周期与 macOS 守护

首条提醒里程碑可以前台 CLI 运行。完整 MVP 提供 macOS `launchd` 配置：

- 登录后自动启动；
- 异常退出后自动重启；
- 标准输出与错误写入指定日志；
- 核心 CLI 保持平台无关；
- 不承诺电脑关机或睡眠期间可用；
- 将来需要真正 24×7 时，应迁移到常在线主机或云服务。

收到 `Ctrl+C`、`SIGTERM` 或重启信号时：

- 不再启动新检查；
- 当前请求允许完成；
- 整个优雅退出最多等待 30 秒；
- 安全状态变更必须原子保存；
- 未开始的通知投递保留到下次启动；
- 正常人工停止返回成功退出码；
- 配置错误、状态损坏等致命错误返回非零退出码。

普通启动和重启只写日志，不发送启动通知。

## 十七、架构边界

核心逻辑应与外部系统隔离：

- `StorefrontAdapter`：负责地区目录与详情观察；MVP 只有 Apple 日本实现；
- `NotificationChannel`：负责一个渠道的通知投递；MVP 有 Discord 和 SMTP Email；
- 核心模块：订阅匹配、商品身份、上架/下架状态机、通知批次、持久化和调度；
- 标准化领域属性不得依赖日文页面原始文案；
- 新增地区或渠道通过代码注册和发布新版本；
- MVP 不支持运行时动态安装插件。

不得为了未来扩展提前实现多用户、数据库、Web UI、消息队列或通用工作流引擎。

## 十八、技术要求

- Python 3.13+
- uv
- Ruff
- Pytest
- Type Hints
- 模块化结构
- 依赖尽量少且职责明确

## 十九、自动化测试与手动验收

### 19.1 自动化测试

最低覆盖：

- 日本目录页解析；
- 日文商品详情页规格解析；
- 空格、连字符和单位标准化；
- 目标配置精确匹配；
- 16 英寸、512GB、2TB、不同内存或核心数不匹配；
- 缺少必需字段时无法评估；
- 首次出现提醒一次；
- 持续在售不重复提醒；
- 一次可信缺席后恢复不提醒；
- 两次可信缺席后重新出现再次提醒；
- 失败检查不累计缺席；
- 多商品批次及 Discord 分段；
- Discord 与 Email 独立成功、失败、重试和过期；
- 状态原子保存、备份、锁和 schema 校验；
- 配置严格校验和 60 秒下限；
- 配置变化、订阅 ID 与去重语义；
- 运行故障首次告警与恢复告警；
- 优雅退出。

自动化测试必须使用本地夹具和假通知端点，不得向 Apple、Discord 或 SMTP 发送真实请求。

### 19.2 手动验收

- 对真实 Apple 日本页面执行 `check-once`；
- 发送 Discord 测试消息；
- 发送 Email 测试消息；
- 在无真实库存时发送明确标注 TEST 的无库存消息；
- 启动一次真实长期监控；
- 验证重启后不会重复提醒；
- 验证 `launchd` 可启动并重启进程。

## 二十、未来规划

未来可能增加：

- Mac mini、Mac Studio、MacBook Air、iPad、iPhone；
- 美国、香港、中国、新加坡等地区；
- LINE、Telegram、Slack、Push Notification、WhatsApp；
- Web Dashboard；
- 当前库存、历史库存、价格变化和通知记录；
- 库存出现频率、持续时间、跨地区比较和历史价格分析；
- 多用户与数据库。

这些能力不属于 MVP，不得阻塞首条提醒或 MVP 的既定完成标准。
